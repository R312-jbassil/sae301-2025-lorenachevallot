---
import Svg from "../components/Svg.astro";
---

<div class="apercu-container">
    <h2>Aperçu</h2>
    <div class="visualization-area">
       <Svg />
        <button class="zoom-btn">
            <svg
                width="24"
                height="24"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
            >
                <circle cx="11" cy="11" r="8"></circle>
                <path d="m21 21-4.35-4.35"></path>
            </svg>
        </button>
    </div>

    <div class="view-buttons">
        <button class="view-btn active">Vue Avant</button>
        <button class="view-btn">Vue de Profil</button>
    </div>

    <button class="info-btn">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
            <circle cx="12" cy="12" r="10"></circle>
            <path d="M12 16v-4M12 8h.01" fill="white"></path>
        </svg>
    </button>
</div>

<style>
    .apercu-container {
        background: white;
        border-radius: 12px;
        padding: 28px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    h2 {
        margin: 0 0 24px 0;
        font-size: 22px;
        font-weight: 600;
        color: #1a1a1a;
    }

    .visualization-area {
        position: relative;
        width: 100%;
        height: 500px;
        background: #f5f5f5;
        border-radius: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 20px;
    }

    .lunettes-img {
        max-width: 80%;
        max-height: 80%;
        object-fit: contain;
    }

    .zoom-btn {
        position: absolute;
        top: 12px;
        right: 12px;
        width: 40px;
        height: 40px;
        border: 1px solid #ddd;
        background: white;
        border-radius: 50%;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.2s ease;
    }

    .zoom-btn:hover {
        background: #f0f0f0;
        border-color: #999;
    }

    .view-buttons {
        display: flex;
        gap: 12px;
        justify-content: flex-end;
        margin-bottom: 16px;
    }

    .view-btn {
        padding: 8px 16px;
        border: none;
        border-radius: 20px;
        background: #000;
        color: white;
        font-size: 14px;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.2s ease;
    }

    .view-btn:hover {
        background: #333;
    }

    .view-btn.active {
        background: #000;
    }

    .info-btn {
        position: absolute;
        bottom: 12px;
        left: 12px;
        width: 32px;
        height: 32px;
        border: none;
        background: #f5f5f5;
        border-radius: 50%;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.2s ease;
    }

    .info-btn:hover {
        background: #e0e0e0;
    }
</style>
